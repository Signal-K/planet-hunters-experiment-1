
# Build artifacts
**/android/build/
**/.cxx/
godot-exports/
android/app/build/
android/app/src/main/assets/
build/
ios/build/
ios/Pods/Target Support Files/
electron-dist/
dist/
/release/
out/
dist/*.blockmap
dist/*.dmg
dist/*.AppImage
dist/*.deb
dist/*.rpm
dist/*.snap
dist/*.exe
dist/*.msi
dist/*.zip
dist/*.tar.gz
dist/*.nupkg
release/*.blockmap
release/*.dmg
release/*.AppImage
release/*.deb
release/*.rpm
release/*.snap
release/*.exe
release/*.msi
release/*.zip
release/*.tar.gz
release/*.nupkg
.cache/electron/
.cache/electron-builder/
scene/supabase.runtime.json

# Godot editor/build cache
**/.godot/

# Root-level Godot build artifacts
GodotTest.pck
GodotTest/

# macOS metadata
.DS_Store
**/.DS_Store

# Node modules
node_modules/
web/.next/
pocketbase/pb_data/
pocketbase/landnam-backend

# Large binaries
*.so
*.a
*.xcframework
*.xcarchive
*.ipa
*.apk
*.aab

.vercel
.env

# Local tooling/runtime state
.godot-bin/
.claude/worktrees/

# Landnam uses the parent Navigation workspace for durable context. Do not
# recreate a child-repository Knowns snapshot, bridge, or proposal outbox.
.knowns/
.knowns-outbox/
.knowns-bridge.json
KNOWNS.snapshot.md
.claude/
.compass/

# UX tour outputs (generated, not source)
ux-screenshots/
test-results/
*.bak

# CI sandbox output
sandbox/output/
__pycache__/
