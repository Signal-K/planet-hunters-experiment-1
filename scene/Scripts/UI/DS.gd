class_name DS

# Surfaces
const VOID        := Color(0.024, 0.035, 0.059)   # #06090f
const BG          := Color(0.039, 0.071, 0.114)   # #0a121d
const SURFACE     := Color(0.071, 0.133, 0.212)   # #122236
const SURFACE_2   := Color(0.094, 0.188, 0.294)   # #18304b
const SURFACE_3   := Color(0.122, 0.239, 0.369)   # #1f3d5e
const OVERLAY     := Color(0.024, 0.035, 0.059, 0.72)

# Primary — Command Cyan
const PRIMARY     := Color(0.247, 0.663, 1.0)     # #3fa9ff
const PRIMARY_B   := Color(0.424, 0.761, 1.0)     # #6cc2ff
const PRIMARY_P   := Color(0.110, 0.529, 0.863)   # #1c87dc
const PRIMARY_S   := Color(0.247, 0.663, 1.0, 0.16)
const PRIMARY_BD  := Color(0.247, 0.663, 1.0, 0.35)

# Highlight — Amber
const AMBER       := Color(0.961, 0.651, 0.137)   # #f5a623
const AMBER_B     := Color(1.0,   0.761, 0.361)   # #ffc25c
const AMBER_P     := Color(0.839, 0.541, 0.051)   # #d68a0d
const AMBER_S     := Color(0.961, 0.651, 0.137, 0.14)
const AMBER_BD    := Color(0.961, 0.651, 0.137, 0.55)

# Status
const STATUS_OK   := Color(0.224, 0.827, 0.416)   # #39d36a
const STATUS_WARN := Color(1.0,   0.702, 0.278)   # #ffb347
const STATUS_CRIT := Color(1.0,   0.353, 0.416)   # #ff5a6a
const STATUS_INFO := Color(0.494, 0.784, 1.0)     # #7ec8ff

# Minerals
const MINERAL_SILICON := Color(0.725, 0.847, 1.0)    # #b9d8ff
const MINERAL_IRON    := Color(0.851, 0.443, 0.314)  # #d97150
const MINERAL_ICE     := Color(0.608, 0.925, 1.0)    # #9becff
const MINERAL_CARBON  := Color(0.416, 0.447, 0.502)  # #6a7280
const MINERAL_GOLD    := Color(1.0,   0.820, 0.400)  # #ffd166
const MINERAL_RARE    := Color(0.753, 0.518, 1.0)    # #c084ff

# Text
const TEXT        := Color(0.902, 0.937, 1.0)     # #e6efff
const TEXT_DIM    := Color(0.663, 0.722, 0.808)   # #a9b8ce
const TEXT_MUTED  := Color(0.365, 0.451, 0.565)   # #5d7390
const TEXT_GHOST  := Color(0.902, 0.937, 1.0, 0.35)

# Borders
const HAIRLINE    := Color(0.247, 0.663, 1.0, 0.18)
const HAIRLINE_S  := Color(0.247, 0.663, 1.0, 0.45)
const DIVIDER     := Color(0.663, 0.722, 0.808, 0.12)

# Radii (pixels)
const R_XS := 2
const R_SM := 4
const R_MD := 6
const R_LG := 8
const R_XL := 14
const R_CARD := 14

# Spacing (8pt rhythm)
const S1 := 4
const S2 := 8
const S3 := 12
const S4 := 16
const S5 := 24
const S6 := 32
const S7 := 48
const S8 := 64

# Font sizes (portrait 1080x1920 canvas)
const F_DISPLAY := 72
const F_TITLE   := 48
const F_H1      := 36
const F_H2      := 28
const F_BODY    := 22
const F_BUTTON  := 20
const F_LABEL   := 16
const F_MICRO   := 13
const F_HEADLINE := 28
